import React from 'react'

//Add auth form that takes in username and password so users can accsess private routes 
const Login = () => {
    return (
        <div>
            Login
        </div>
    )
}

export default Login
